import {useRouter} from "next/router";

interface Props {
    
}

const BackButton = (props: Props) => {
    return (
        
        <button className="">
            back
        </button>
    )
}

export default BackButton
